services:
  - type: web
    name: clearhr-agentic-hr-assistant
    runtime: python
    plan: free
    # Build/cache the backend selected by the non-secret RAG_BACKEND setting.
    # Tests stay hermetic on lexical; a dense build itself verifies the local
    # ONNX model can be downloaded and indexed before the service starts.
    buildCommand: pip install -r requirements.txt && python scripts/build_rag_index.py && RAG_BACKEND=lexical python -m pytest -q
    startCommand: uvicorn app.main:app --host 0.0.0.0 --port $PORT
    healthCheckPath: /health
    # Render waits for the repository's GitHub checks before starting an
    # automatic deploy; its own build command then reruns the test suite.
    autoDeployTrigger: checksPass
    envVars:
      - key: PYTHONUNBUFFERED
        value: "1"
      - key: RAG_BACKEND
        # Dense mode needs a measured host-memory/cold-start trial first.
        # Change this to dense only for that explicit staging/deployment run.
        value: lexical
